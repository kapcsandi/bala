<% title @page.title %>
<%= @page.body %>
<% if admin? %>
<p>
  <%= link_to t('admin.edit'), edit_page_path(@page) %> |
  <%= link_to t('admin.destroy'), @page, :confirm => t('admin.are_you_sure'), :method => :delete %> |
  <%= link_to t('admin.view_all'), pages_path %>
</p>
<% end %>
