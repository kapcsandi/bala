page.select('#houses_bookings input').first.value = @houses[0].code
if @houses.size == 1
  page.replace_html 'house_stamp', :partial => 'stamp', :locals => { :house => @houses[0], :last => true, :open => false}
  page['house_stamp'].show
else
  page['house_stamp'].hide
end